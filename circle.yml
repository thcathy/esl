general:
  artifacts:
    - "target/ESL.war"
    
machine:
  services:
    - docker
  java:
    version:
      oraclejdk8

    
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
    - mvn package -DskipTests

deployment:
  hub:
    branch: master
    commands:
      - docker build -t thcathy/esl docker/Dockerfile_esl
      - docker build -t thcathy/esl-mysql docker/Dockerfile_esl-mysql
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push thcathy/esl
      - docker push thcathy/esl-mysql

