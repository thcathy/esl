
alphaNumericOnly = Only allow alphabetic or numeric characters

alphaOnly = Only allow alphabetic characters

containInvalidChar = contain of invalid character(s)

incorrectSize = Length should between {min} and {max}

invalidAddress = Invalid Address

invalidEmail = Invalid Email Address

overSize = Length over {value}
